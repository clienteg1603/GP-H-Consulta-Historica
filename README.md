# GP-H Android — canal de atualização

Esta branch é gerada automaticamente. O APK, o manifesto `latest.json` e a chave de assinatura alpha são usados pelo mecanismo de atualização do aplicativo Android.
